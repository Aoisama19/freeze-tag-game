namespace BarafPaani.Gameplay
{
    /// <summary>What a contact between two characters should do, if anything.</summary>
    public enum TagOutcome
    {
        None,
        Freeze,
        Unfreeze
    }
}
